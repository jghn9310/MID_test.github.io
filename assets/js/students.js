/* Student content is pre-rendered from the validated local snapshot.
   This file intentionally performs no network request and no HTML rendering. */
(() => {
  "use strict";
  window.MidSecurity?.hardenBlankLinks(document);
})();
